language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t benmcdermott/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true benmcdermott/docker-react npm run test